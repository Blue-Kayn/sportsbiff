<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "SportsBiff" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-gray-900 text-white">
    <% if user_signed_in? %>
      <%= render "shared/navbar" %>
    <% end %>

    <% if notice.present? %>
      <div class="fixed top-4 right-4 z-50 bg-green-600 text-white px-4 py-2 rounded-lg shadow-lg" data-controller="flash" data-flash-target="message">
        <%= notice %>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="fixed top-4 right-4 z-50 bg-red-600 text-white px-4 py-2 rounded-lg shadow-lg" data-controller="flash" data-flash-target="message">
        <%= alert %>
      </div>
    <% end %>

    <main class="<%= user_signed_in? ? '' : 'min-h-screen' %>">
      <%= yield %>
    </main>
  </body>
</html>
