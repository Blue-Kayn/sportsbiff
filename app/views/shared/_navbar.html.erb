<nav class="bg-gray-900 border-b border-gray-800">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between items-center" style="height: 129px;">
      <!-- Left logo -->
      <div class="flex items-center" style="margin-left: -24%;">
        <%= link_to root_path, class: "flex items-center" do %>
          <img src="/logo.png" alt="SportsBiff" style="height: 158px; width: auto;">
        <% end %>
      </div>

      <!-- Right nav items -->
      <div class="flex items-center space-x-4">
        <% if user_signed_in? %>
          <div class="text-base text-gray-400">
            <%= current_user.queries_remaining %>/<%= current_user.daily_limit %> queries
          </div>
          <%= link_to "Chat", chats_path, class: "text-gray-300 hover:text-white px-3 py-2 text-base font-medium" %>
          <%= link_to profile_path, class: "text-gray-300 hover:text-white px-3 py-2 text-base font-medium flex items-center gap-1" do %>
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"></path>
            </svg>
            Profile
          <% end %>
          <%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "text-gray-300 hover:text-white px-3 py-2 text-base font-medium bg-transparent border-0 cursor-pointer" %>
        <% else %>
          <%= link_to "Sign In", new_user_session_path, class: "text-gray-300 hover:text-white px-3 py-2 text-base font-medium" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-lg text-base font-medium" %>
        <% end %>
      </div>
    </div>
  </div>
</nav>
