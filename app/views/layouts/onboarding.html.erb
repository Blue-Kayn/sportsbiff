<!DOCTYPE html>
<html class="h-full">
  <head>
    <title><%= content_for(:title) || "Get Started - SportsBiff" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="h-full bg-gray-900 text-white">
    <div class="min-h-screen flex flex-col">
      <!-- Header -->
      <header class="py-6 px-4">
        <div class="max-w-2xl mx-auto flex items-center justify-between">
          <div class="text-2xl font-bold text-blue-500">SportsBiff</div>
          <div class="text-gray-400 text-sm">
            Signed in as <%= current_user.email %>
          </div>
        </div>
      </header>

      <% if alert.present? %>
        <div class="max-w-2xl mx-auto px-4 mb-4">
          <div class="bg-red-600/20 border border-red-500 text-red-300 px-4 py-3 rounded-lg">
            <%= alert %>
          </div>
        </div>
      <% end %>

      <!-- Main Content -->
      <main class="flex-1 flex items-start justify-center px-4 py-8">
        <div class="w-full max-w-2xl">
          <%= yield %>
        </div>
      </main>

      <!-- Footer -->
      <footer class="py-6 px-4 text-center text-gray-500 text-sm">
        Your personalized sports companion
      </footer>
    </div>
  </body>
</html>
